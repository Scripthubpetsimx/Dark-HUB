local Luna = loadstring(game:HttpGet("https://raw.githubusercontent.com/Nebula-Softworks/Luna-Interface-Suite/refs/heads/master/source.lua", true))()

local Window = Luna:CreateWindow({
	Name = "ExecutiveRat",
	Subtitle = nil,
	LogoID = "82795327169782",
	LoadingEnabled = true,
	LoadingTitle = "PREMIUM SCRIPT",
	LoadingSubtitle = "by ExecutiveRAT",
	ConfigSettings = {
		RootFolder = nil,
		ConfigFolder = "EXECUTIVERAT"
	},
	KeySystem = false  -- Key system disabled
})

local Tab = Window:CreateTab({
	Name = "Main",
	Icon = "view_in_ar",
	ImageSource = "Material",
	ShowTitle = true
})

local Button = Tab:CreateButton({
	Name = "DUPER",
	Description = "DUPER Script, used to duplicate EGGS, PETS and SEEDPACKS",
	Callback = function()
		Luna:Notification({ 
			Title = "EXECUTIVERAT",
			Content = "Has been PATCHED TODAY, for those who used it TRADE IT FAST, it will be GONE 🤑"
		})
	end
})
local Button = Tab:CreateButton({
    Name = "Server Scanner",
    Description = "SERVER SCANNER, know your luck !!",
    Callback = function()
        -- Create a dummy script parent to satisfy the original script's requirements
        local fakeScript = {
            Parent = {
                Parent = {
                    Parent = game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui")
                }
            }
        }
        
        -- Load and adapt the scanner script
        local scannerScript = loadstring(game:HttpGet("https://raw.githubusercontent.com/Scripthubpetsimx/Dark-HUB/refs/heads/main/deepseek_lua_20250621_fb7786.lua"))()
        
        -- Run the scanner with our fake environment
        setfenv(scannerScript, {
            script = fakeScript,
            game = game,
            require = require,
            wait = task.wait,
            pcall = pcall,
            print = print,
            warn = warn,
            error = error,
            getfenv = getfenv,
            setfenv = setfenv,
            getmetatable = getmetatable,
            setmetatable = setmetatable,
            next = next,
            pairs = pairs,
            ipairs = ipairs,
            select = select,
            tonumber = tonumber,
            tostring = tostring,
            type = type,
            unpack = unpack,
            Vector2 = Vector2,
            Vector3 = Vector3,
            Color3 = Color3,
            UDim2 = UDim2,
            UDim = UDim,
            Rect = Rect,
            Axes = Axes,
            Faces = Faces,
            BrickColor = BrickColor,
            CFrame = CFrame,
            Enum = Enum,
            Instance = Instance,
            math = math,
            string = string,
            table = table,
            coroutine = coroutine,
            os = os,
            debug = debug,
            _G = _G,
            shared = shared,
            script = fakeScript
        })
        
        scannerScript()
        
        Luna:Notification({ 
            Title = "EXECUTIVERAT",
            Content = "SERVER SCANNER LOADED"
        })
    end
})
local Button = Tab:CreateButton({
	Name = "No-lag OFICCIAL",
	Description = "Free No Lag v1 | Changelog = [+] Auto Craft",
	Callback = function()
		loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/NoLag-id/No-Lag-HUB/refs/heads/main/Loader/LoaderV1.lua"))()
		Luna:Notification({ 
			Title = "EXECUTIVERAT",
			Content = "No-Lag is running"
		})
	end
})
local Button = Tab:CreateButton({
	Name = "No-lag OFICCIAL FREE/Premium",
	Description = "The BEST SCRIPT EVER - Key",
	Callback = function()
		script_key = ""
		loadstring(game:HttpGet("https://raw.githubusercontent.com/NoLag-id/No-Lag-HUB/refs/heads/main/Loader/LoaderV3.lua"))()
		Luna:Notification({ 
			Title = "EXECUTIVERAT",
			Content = "No-Lag is running"
		})
	end
})
local antiAfkConnection
local Toggle = Tab:CreateToggle({
	Name = "Enable Anti-AFK",
	Callback = function(Value)
		if Value then
			antiAfkConnection = game:GetService("Players").LocalPlayer.Idled:Connect(function()
				game:GetService("VirtualUser"):ClickButton2(Vector2.new())
			end)
		elseif antiAfkConnection then
			antiAfkConnection:Disconnect()
		end
	end
}, "AntiAFK")

-- Add configuration section
Tab:BuildConfigSection()

-- MUST be at bottom to load saved settings
Luna:LoadAutoloadConfig()


local Tab = Window:CreateTab({
	Name = "Troll",
	Icon = "view_in_ar",
	ImageSource = "Material",
	ShowTitle = true
})

local Button = Tab:CreateButton({
	Name = "Revenge Hand",
	Description = "Transform your Roblox Character into a MONSTER with a big ahh HANDS",
	Callback = function()
	  loadstring(game:HttpGet("https://raw.githubusercontent.com/Scripthubpetsimx/guiscript/refs/heads/main/reevenge%20hands.txt"))() 
		Luna:Notification({ 
			Title = "EXECUTIVERAT",
			Content = "Transforming into..."
		})
	end
})
local Button = Tab:CreateButton({
	Name = "UFWboo",
	Description = "Get an ANIMATION TOOL, check your inventory for IT",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/LolnotaKid/SCRIPTSBYVEUX/main/UFWboo.txt"))()
		Luna:Notification({ 
			Title = "EXECUTIVERAT",
			Content = "LolnotaKid UFWboo !!"
		})
	end
})

local Button = Tab:CreateButton({
	Name = "SUS ANIMATIONS",
	Description = "Cool animations best for trolling ;)",
	Callback = function()
		loadstring(game:HttpGet("https://raw.githubusercontent.com/p0e1/1/refs/heads/main/Fe%20R6%20Animation"))()
		Luna:Notification({ 
			Title = "EXECUTIVERAT",
			Content = "Do not jerk too much !!"
		})
	end
})
local Button = Tab:CreateButton({
	Name = "PHYSCO",
	Description = "A walk Animation",
	Callback = function()
loadstring(game:HttpGet("https://pastefy.app/YJITpCRA/raw"))()
		Luna:Notification({ 
			Title = "EXECUTIVERAT",
			Content = "Do not jerk too much !!"
		})
	end
})
local Button = Tab:CreateButton({
	Name = "KILLER",
	Description = "A walk Animation",
	Callback = function()
		loadstring(game:HttpGet("https://pastefy.app/riYch0rX/raw"))() 
		Luna:Notification({ 
			Title = "EXECUTIVERAT",
			Content = "Do not jerk too much !!"
		})
	end
})


-- MUST be at bottom to load saved settings
Luna:LoadAutoloadConfig()
