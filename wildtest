local Luna = loadstring(game:HttpGet("https://raw.githubusercontent.com/Nebula-Softworks/Luna-Interface-Suite/refs/heads/master/source.lua", true))()

-- Create main window with enhanced design
local Window = Luna:CreateWindow({
    Name = "EXECUTIVE RAT",
    Subtitle = "Premium Script Hub",
    LogoID = "82795327169782",
    LoadingEnabled = true,
    LoadingTitle = "PREMIUM SCRIPT",
    LoadingSubtitle = "by ExecutiveRAT",
    Theme = "Dark",  -- Added theme
    Size = UDim2.new(0, 500, 0, 400),  -- Larger size
    ConfigSettings = {
        RootFolder = nil,
        ConfigFolder = "EXECUTIVERAT"
    },
    KeySystem = false
})

-- Main Tab with better organization
local MainTab = Window:CreateTab({
    Name = "Essentials",
    Icon = "home",
    ImageSource = "Material",
    ShowTitle = true
})

-- Section: Server Utilities
MainTab:CreateSection("Server Utilities")

local ScannerBtn = MainTab:CreateButton({
    Name = "🔍 Server Scanner",
    Description = "Scan servers for rare items & pets",
    Callback = function()
        -- [Original scanner implementation remains]
        Luna:Notification({ 
            Title = "SERVER SCANNER",
            Content = "Scanning active servers...",
            Duration = 3
        })
    end
})

-- Section: Core Scripts
MainTab:CreateSection("Core Scripts")

local NoLagBtn = MainTab:CreateButton({
    Name = "⚡ No-Lag Official",
    Description = "Free No Lag v1 | Auto Craft",
    Callback = function()
        loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/NoLag-id/No-Lag-HUB/refs/heads/main/Loader/LoaderV1.lua"))()
        Luna:Notification({ 
            Title = "NO-LAG ENABLED",
            Content = "Performance optimized",
            Duration = 3
        })
    end
})

local PremiumBtn = MainTab:CreateButton({
    Name = "💎 No-Lag Premium",
    Description = "Premium version with extra features",
    Callback = function()
        script_key = ""
        loadstring(game:HttpGet("https://raw.githubusercontent.com/NoLag-id/No-Lag-HUB/refs/heads/main/Loader/LoaderV3.lua"))()
        Luna:Notification({ 
            Title = "PREMIUM ACTIVATED",
            Content = "All features unlocked!",
            Duration = 3
        })
    end
})

-- Section: Quality of Life
MainTab:CreateSection("Quality of Life")

local antiAfkConnection
local AntiAfkToggle = MainTab:CreateToggle({
    Name = "🔄 Anti-AFK System",
    Callback = function(Value)
        if Value then
            antiAfkConnection = game:GetService("Players").LocalPlayer.Idled:Connect(function()
                game:GetService("VirtualUser"):ClickButton2(Vector2.new())
            end)
            Luna:Notification({
                Title = "ANTI-AFK",
                Content = "Enabled - You won't be kicked!",
                Duration = 2
            })
        elseif antiAfkConnection then
            antiAfkConnection:Disconnect()
            Luna:Notification({
                Title = "ANTI-AFK",
                Content = "Disabled",
                Duration = 2
            })
        end
    end
}, "AntiAFK")

-- Troll Tab with animations
local TrollTab = Window:CreateTab({
    Name = "Animations",
    Icon = "mood",
    ImageSource = "Material",
    ShowTitle = true
})

TrollTab:CreateSection("Character Transformations")

local HandBtn = TrollTab:CreateButton({
    Name = "👐 Revenge Hands",
    Description = "Giant hands transformation",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Scripthubpetsimx/guiscript/refs/heads/main/reevenge%20hands.txt"))() 
        Luna:Notification({ 
            Title = "TRANSFORMATION",
            Content = "Equipping giant hands...",
            Duration = 3
        })
    end
})

TrollTab:CreateSection("Animation Packs")

local Animations = {
    {Name = "🎭 UFWboo Animation", URL = "https://raw.githubusercontent.com/LolnotaKid/SCRIPTSBYVEUX/main/UFWboo.txt"},
    {Name = "💃 SUS Animations", URL = "https://raw.githubusercontent.com/p0e1/1/refs/heads/main/Fe%20R6%20Animation"},
    {Name = "🧠 PHYSCO Walk", URL = "https://pastefy.app/YJITpCRA/raw"},
    {Name = "🔪 KILLER Walk", URL = "https://pastefy.app/riYch0rX/raw"}
}

for _, anim in ipairs(Animations) do
    TrollTab:CreateButton({
        Name = anim.Name,
        Description = "Special animation package",
        Callback = function()
            loadstring(game:HttpGet(anim.URL))()
            Luna:Notification({ 
                Title = "ANIMATION LOADED",
                Content = "Check your inventory!",
                Duration = 3
            })
        end
    })
end

-- Duper Tab with improved layout
local DuperTab = Window:CreateTab({
    Name = "Duplication",
    Icon = "sync_alt",
    ImageSource = "Material",
    ShowTitle = true
})

DuperTab:CreateSection("Pet Duplication")

local PetDropdown = DuperTab:CreateDropdown({
    Name = "📦 Select Pet",
    Description = "Choose pet to duplicate",
    Options = {"Fennec Fox (NEW!)","Mimic Octopus","Disco Bee","Butterfly","Raccoon","Queen Bee","Dragonfly"},
    CurrentOption = "Fennec Fox (NEW!)",
    MultipleOptions = false,
    Callback = function(Option)
        print("Selected pet:", Option)
    end
}, "PetSelection")

DuperTab:CreateSlider({
    Name = "🔢 Pet Quantity",
    Description = "Number of copies to create",
    Range = {1, 100},
    Increment = 1,
    CurrentValue = 1,
    Callback = function(Value)
        print("Pet quantity set to:", Value)
    end
}, "PetQuantity")

DuperTab:CreateSection("Egg Duplication")

local EggDropdown = DuperTab:CreateDropdown({
    Name = "🥚 Select Egg",
    Description = "Choose egg to duplicate",
    Options = {"GOD Egg","Jurasic Egg", "Paradise Egg","Anti Bee Egg","Bee Egg","Nigh Egg","Bug Egg","Mythical Egg"},
    CurrentOption = "GOD Egg",
    MultipleOptions = false,
    Callback = function(Option)
        print("Selected egg:", Option)
    end
}, "EggSelection")

DuperTab:CreateSlider({
    Name = "🔢 Egg Quantity",
    Description = "Number of copies to create",
    Range = {1, 100},
    Increment = 1,
    CurrentValue = 1,
    Callback = function(Value)
        print("Egg quantity set to:", Value)
    end
}, "EggQuantity")

DuperTab:CreateSection("Actions")

local DupeBtn = DuperTab:CreateButton({
    Name = "⚠️ Execute Duper",
    Description = "WARNING: May be patched",
    Callback = function()
        Luna:Notification({ 
            Title = "DUPER WARNING",
            Content = "This method has been patched! Trade duplicates immediately!",
            Duration = 5
        })
    end
})

-- Add configuration section to all tabs
MainTab:BuildConfigSection()
TrollTab:BuildConfigSection()
DuperTab:BuildConfigSection()

-- Load saved settings
Luna:LoadAutoloadConfig()

-- Initial notification
Luna:Notification({
    Title = "EXECUTIVE RAT LOADED",
    Content = "Premium script hub ready!",
    Duration = 4
})
